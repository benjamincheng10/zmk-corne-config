on:
  push:
    paths-ignore:
      - "keymap-drawer/**"
      - "*.md"
  pull_request:
    paths-ignore:
      - "keymap-drawer/**"
      - "*.md"
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
